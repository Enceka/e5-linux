# networkd leaves wlan0 alone: it is a port of br0, placed there by hostapd
# (bridge=br0), and br0 holds the addresses (opt/e5/net-bridge.sh).  Kept, not
# deleted, for the same reason as 10-e5-usb0.network.
[Match]
Name=wlan0

[Link]
Unmanaged=yes
