# networkd leaves wlan0 alone: NetworkManager owns it (the "Hotspot" AP, a port
# of br0, or a Wi-Fi network joined from Phosh), and br0 holds the LAN
# addresses (opt/e5/net-bridge.sh).  Kept, not
# deleted, for the same reason as 10-e5-usb0.network.
[Match]
Name=wlan0

[Link]
Unmanaged=yes
