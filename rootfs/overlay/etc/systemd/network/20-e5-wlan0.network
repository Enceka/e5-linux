[Match]
Name=wlan0

[Network]
Address=192.168.9.1/24
# DHCP and DNS for the hotspot come from dnsmasq (etc/dnsmasq.d/e5-hotspot.conf): networkd
# can hand out addresses but cannot answer the DNS queries it would advertise.
DHCPServer=no
