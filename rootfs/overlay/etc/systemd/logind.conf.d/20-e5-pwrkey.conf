# The power key cannot suspend this device: the modem's data path refuses it
# ("sipa 25220000.sipa: thread prepare suspend err" on every attempt), so
# HandlePowerKey=suspend or gsd's power-button-action=suspend do nothing visible.
# logind's "lock" gives the phone behaviour instead: short press locks the session,
# which is phosh's lock screen.
#
# Long press stays ignored *here* -- logind's default for it is poweroff, which is easy
# to trigger by accident -- but it is not left unused: /opt/e5/powerkey.py watches the
# same key and, after a deliberate 1.5 s hold, opens gnome-session's "Power Off"
# dialog (see docs/FINDINGS.md section 18).
[Login]
HandlePowerKey=lock
HandlePowerKeyLongPress=ignore
