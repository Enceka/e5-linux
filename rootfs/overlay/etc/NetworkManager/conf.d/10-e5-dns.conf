# The baseband link owns /etc/resolv.conf: e5-mobile-data writes the DNS servers
# the network handed out in AT+CGCONTRDP.  Left to itself NetworkManager rewrites
# the file on every reload and name resolution dies.
[main]
dns=none
